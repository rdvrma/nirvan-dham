const puppeteer = require('puppeteer-core'); (async () => { const browser = await puppeteer.launch({ executablePath: 'C:\\\\Program Files\\\\Google\\\\Chrome\\\\Application\\\\chrome.exe', headless: 'new', args: ['--ignore-certificate-errors', '--no-sandbox'] }); const page = await browser.newPage(); page.on('pageerror', err => console.log('PAGE_ERROR:', err.message, err.stack)); page.on('console', msg => { if (msg.type() === 'error') console.log('CONSOLE_ERROR:', msg.text(), msg.location()); }); await page.goto('https://nirvandham.in', { waitUntil: 'networkidle0', timeout: 30000 }).catch(e => console.log('GOTO_ERROR:', e.message)); await new Promise(r => setTimeout(r, 2000)); const html = await page.content(); if (html.includes('__next_error__')) console.log('CRASH DETECTED IN HTML'); await browser.close(); })();
